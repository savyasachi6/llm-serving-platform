name: "short_chat"
description: "Baseline short multi-turn chat"
concurrency: 10
requests: 100
workload_type: "chat"
payload:
  model: "test-model"
  messages:
    - role: "user"
      content: "Hello, what is the capital of France?"
