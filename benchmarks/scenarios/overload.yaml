name: "overload"
description: "Saturation test to verify admission rejection (HTTP 429/503)"
concurrency: 100
requests: 1000
workload_type: "chat"
payload:
  model: "Qwen/Qwen2.5-0.5B-Instruct"
  messages:
    - role: "user"
      content: "Hello, what is the capital of France?"
  max_tokens: 50
